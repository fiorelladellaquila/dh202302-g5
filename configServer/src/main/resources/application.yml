server:
  port: 8888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AgustinDamelio/BackEnd1_Group5_config
          default-label: main
          force-pull: true
          search-paths: configData
