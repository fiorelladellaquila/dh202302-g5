server:
  port: {PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/dh202302-g5-config
          default-label: task-2-config
          force-pull: true
          search-paths: configData
