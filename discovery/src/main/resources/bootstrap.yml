#server:
#  port: ${PORT:8761}

spring:
  application:
    name: ms-discovery
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registerWithEureka: false
#    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://${eureka.instance}:${server.port}/eureka/